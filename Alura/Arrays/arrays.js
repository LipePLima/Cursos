const notas = [10, 6.5, 8, 7.5];

let media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length;

console.log(media)

// Os principais métodos de arrays

    // concat()

    const notas2 = [2, 6, 8]
    let notas3 = notas2.concat(notas)

    console.log(notas3)
